ngraph react component
=======================
This here is a wrapper around ngraph so we can use it as a react component.

Read about [ngraph](https://www.npmjs.com/package/ngraph.graph)

an example renderer of ngraph is [ngraph.sigma](https://github.com/deanshub/ngraph.sigma.git), use it.
